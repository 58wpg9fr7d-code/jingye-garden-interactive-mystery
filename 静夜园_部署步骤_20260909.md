# 静夜园 · 部署上线步骤（2026-09-09）

## 当前状态
- 本地仓库：`/Users/humphcey/Desktop/工作/项目/项目3-静夜园互动推理/源码与部署`（已连 GitHub）
- 已整理成一个干净提交，包含：
  - 手机端新版本：碎裂开屏 + 第一幕·来到庄园过场 + 新版角色选择页（通行密码 + 角色下拉 + 灰色"进入庄园"按钮），已移除主持人控制台
  - 碎裂素材图 `intro-crack-01 ~ 04`
  - 合并网址：移除 Cloudflare 旧链接，统一为 Vercel 唯一入口
  - 清理垃圾：构建产物备份目录、.docx 内部文档、备份文件、吉祥物文件已排除（不进仓库）
  - 修复 `wrangler.json` 与 `vite.config.ts` 重复 `nodejs_compat` 导致构建报错的问题
- 本地预览已验证：`http://localhost:3001/phone` 返回 200，开屏已是碎裂动画

## 你本机执行（沙盒连不上 GitHub，必须你电脑跑）
在终端粘贴这 3 行：
```
cd /Users/humphcey/Desktop/工作/项目/项目3-静夜园互动推理/源码与部署
git fetch origin
git push --force-with-lease origin main
```
说明：
- `git fetch` 拉取线上最新
- `git push --force-with-lease` 把本地干净版**强制对齐**到 GitHub main（覆盖线上旧历史；仓库只有你一人用，安全）

⚠️ 如果你记得 GitHub 上有重要内容不在这个桌面目录里，先别推，跟我说。

## push 后验证
1. 等 1~3 分钟，Vercel 通常会自动重新部署（前提是 Vercel 连了 GitHub 自动部署）。
2. 打开 `https://murder-mystery-studio.vercel.app/phone` 看新版本。
3. 若 Vercel 没自动更新，去 Vercel 控制台对该项目点 Redeploy。

## 收尾（需你本机 / 网页操作，我无凭证）
1. **撤销一次性 token**：GitHub → 右上角头像 → Settings → Developer settings → Personal access tokens → Fine-grained tokens → 找到那条 → Revoke。
2. **删 Cloudflare Worker**：Cloudflare 控制台 → Workers & Pages → 找到 `jingye-garden-interactive-mystery` → 删除（线上旧备用入口随之失效，不影响 Vercel 主站）。
3. （次要，下次弄）某 docs 里 `/puzzle` 死链待清。
