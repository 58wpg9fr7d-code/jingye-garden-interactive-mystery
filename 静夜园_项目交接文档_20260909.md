# 静夜园互动推理 · 项目交接文档

> 生成日期：2026-09-09
> 用途：把当前项目状态、已完成的改动、以及必须由本人操作的收尾事项，一次性交代清楚。
> 适用对象：本人后续接手，或转交他人处理。

---

## 一、项目是什么

- **名称**：静夜园互动推理（手机端推理剧情体验 / 剧本杀式互动站）
- **GitHub 仓库**：`58wpg9fr7d-code/jingye-garden-interactive-mystery`（公开仓库）
- **唯一正式线上地址**：`https://murder-mystery-studio.vercel.app/phone`
- **部署方式**：Vercel 监听该仓库，从根目录下的 `完整手机端源码/` 子目录构建并发布
- **框架**：vinext（Next.js 风格），本地启动 `npm run dev`，配置含 `wrangler.json` + `vite.config.ts`

---

## 二、当前状态（截至 2026-09-09）

| 项 | 状态 |
|---|---|
| 本地工作仓库 | `/Users/humphcey/Desktop/工作/项目/项目3-静夜园互动推理/源码与部署/`（含 .git） |
| 本地 HEAD | `434d8c0`（干净提交，内容已就绪） |
| GitHub 线上 main | 仍是旧版（约 `7c4e960e`），**尚未对齐** |
| 本地与远程关系 | 本地比远程超前，属于平行提交，需强制对齐 |
| 本地预览 | 此前 `http://localhost:3001/phone` 返回 200、已含新版开屏（dev server 可能已休眠） |

---

## 三、本次已完成的改动（已落本地，未推）

1. **开屏动画改为"手机碎裂"效果**：用 `public/script-images/intro-crack-0*.png/jpg` 系列素材做裂屏序列。
2. **新增"第一幕·来到庄园"过场动画**：放在角色选择之后、手机桌面之前播放（约 3.6s 自动推进，可跳过）。
3. **角色选择页重构**：通行密码输入框 + 角色下拉（向沉 / 胡谋 / 章貘 / 朱渴焰 / 牛守拙）+ 灰色"进入庄园 ›"按钮（密码和角色都选了才点亮）。
4. **删除主持人控制台（DM console）**：相关入口、函数、渲染分支已全部移除（按你的要求，控制台本身你已删，我只负责清代码）。
5. **URL 合并为唯一入口**：各文档 / README 里的 Cloudflare Workers 旧链接已全部去除，只保留 Vercel 主地址。
6. **仓库垃圾清理**：`完整手机端源码/.gitignore` 已扩展，排除 `dist_旧包留底_*`、`*.docx`、`page.tsx.备份_*`、吉祥物相关文件（`MASCOT.md`、`mascot-config.ts`、`components/`、`public/mascot/`）。
7. **修复构建冲突**：`wrangler.json` 里与 `vite.config.ts` 重复的 `nodejs_compat`（compatibility_flags）已移除，否则 Vercel 构建会报错。

> 涉及的主文件：
> - `完整手机端源码/app/phone/page.tsx`（主页面逻辑）
> - `完整手机端源码/app/phone/retro-overrides.css`（样式）
> - `完整手机端源码/wrangler.json`、`vite.config.ts`
> - `README.md`、各 `links.md` / `00-材料索引.md`

---

## 四、为什么必须由你本机操作（AI 能力边界）

AI 运行在隔离沙盒中，存在以下硬限制：
- **没有 computer use 能力**：碰不到你本机的图形界面，无法替你点 GUI。
- **连不上 GitHub**：沙盒网络对 `github.com` 的 git 协议返回 502，无法 `git push`。
- **API 也被中断**：即使持有 token，沙盒侧 `api.github.com` 调用也返回 502 ECONNRESET。

因此以下三件事**只能你本人操作**：① 推代码；② 撤销 token；③ 删 Cloudflare Worker。

---

## 五、你必须执行的步骤（按顺序）

### 步骤 1：推送并触发 Vercel 重新部署

```bash
cd /Users/humphcey/Desktop/工作/项目/项目3-静夜园互动推理/源码与部署
git fetch origin
git push --force-with-lease origin main
```

- 说明：`--force-with-lease` 比 `--force` 安全，只有你本地 fetch 后远程没被别人改动时才覆盖。本仓库仅你一人使用，安全。
- push 成功后等 1~3 分钟，Vercel 自动重部署。
- 验证：访问 `https://murder-mystery-studio.vercel.app/phone`，确认开屏是碎裂动画、角色页是密码+下拉、无主持人控制台。

### 步骤 2：撤销一次性 Fine-grained Token

路径：GitHub → 右上角头像 → **Settings** → **Developer settings** → **Personal access tokens** → **Fine-grained tokens** → 找到以 `github_pat_11CGS4...` 开头那条 → **Revoke**。

> 这条 token 是临时授权用的，用完即废，建议撤销避免泄露。

### 步骤 3（可选）：删除 Cloudflare Worker

路径：Cloudflare 控制台 → **Workers & Pages** → 找到 `jingye-garden-interactive-mystery` → **Delete**。

> 文档里的旧链接已经清干净，删不删不影响线上主站，但有洁癖建议删。

---

## 六、本地预览（可选验证用）

dev server 此前在 `http://localhost:3001/phone` 运行（**3000 端口被另一个项目 `AI产品作品集` 占用，所以本地用 3001**）。若需重启：

```bash
cd /Users/humphcey/Desktop/工作/项目/项目3-静夜园互动推理/源码与部署/完整手机端源码
env -u HTTP_PROXY -u HTTPS_PROXY npm run dev
```

> 注意：`env -u HTTP_PROXY` 是为了让 dev server 不走代理，否则可能连不上本地资源。

---

## 七、遗留 / 待确认事项

1. **吉祥物文件**：已按你的决定排除、未推。若确认它是网站正式功能，后续单独 `git add` 那几个文件再推一次即可。
2. **线上 Vercel 曾独立部署**：线上旧版不完全在 GitHub 历史里，push 对齐后会被本地干净版接管，属预期行为。
3. **死链 `/puzzle`**：`源码与部署/docs/部署与静态版同步清单.md` 里仍残留该死链引用，次要，可后续顺手清。
4. **未用的 DMConsole 函数体**：`page.tsx` 里 DMConsole 函数定义虽不再被渲染，但函数体还留着（无害）。若要彻底干净可删，但不影响运行。

---

## 八、关键文件速查

| 内容 | 路径 |
|---|---|
| 主页面源码 | `完整手机端源码/app/phone/page.tsx` |
| 样式 | `完整手机端源码/app/phone/retro-overrides.css` |
| 碎裂素材 | `完整手机端源码/public/script-images/intro-crack-*.png/.jpg` |
| 构建配置 | `完整手机端源码/wrangler.json`、`vite.config.ts` |
| 本地仓库根 | `/Users/humphcey/Desktop/工作/项目/项目3-静夜园互动推理/源码与部署/` |
| 旧部署清单（早先版） | 桌面 `静夜园_部署步骤_20260909.md` |

---

## 九、一句话总结

本地代码已经是干净可上线的版本（碎裂开屏 + 第一幕过场 + 新角色选择 + 删控制台 + 单 URL），只差你本机三行 push 命令 + 撤销 token + 删 CF Worker 三件事，全部做完即全部收尾。
